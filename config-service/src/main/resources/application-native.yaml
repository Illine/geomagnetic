spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
server:
  port: 8888
  servlet:
    context-path: /config-service