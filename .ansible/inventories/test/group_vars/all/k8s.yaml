namespace: test

deployment:
  replicas: 1
  restart_policy: Always

  health:
    progress_deadline_seconds: 160 # 10 + initial_delay_seconds + (period_seconds * failure_threshold)
    initial_delay_seconds: 90
    period_seconds: 10
    timeout_seconds: 5
    failure_threshold: 6